version: '3.9'
services:
  redis:
    image: redis:latest
    container_name: vsc-redis
    ports:
      - "6380:6380"
    volumes:
      - redis_data:/data
    command: ["redis-server", "--appendonly", "yes"]
volumes:
  redis_data: